type id = number | string;

let idCustomer: id;

idCustomer = 10;
idCustomer = "sam";

let idProduct: id;

idProduct = 200;
idProduct = "sam"

// nos permite definir tipos com qualquer nome, como um apelido.